# Netlify Configuration
# T Board Games & Cafe Website

[build]
  # No build command needed for static site
  publish = "."

# Custom headers for all pages
[[headers]]
  for = "/*"
  [headers.values]
    # Security headers
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

    # Cache control for HTML (no cache to ensure fresh content)
    Cache-Control = "no-cache"

# Cache static assets aggressively
[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Fonts cache
[[headers]]
  for = "/fonts/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# 404 page handling
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404

# Redirect www to non-www (optional - uncomment when domain is set)
# [[redirects]]
#   from = "https://www.tboardgames.com/*"
#   to = "https://tboardgames.com/:splat"
#   status = 301
#   force = true
